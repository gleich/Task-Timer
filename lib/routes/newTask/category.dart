// 📱 Flutter imports:
import 'package:flutter/material.dart';

class NewTaskCategoryRoute extends StatelessWidget {
  final String taskName;

  NewTaskCategoryRoute(this.taskName);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Hello"),
    );
  }
}
